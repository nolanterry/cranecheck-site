"use client";

// Social proof toast removed — previously contained fabricated user activity notifications.
// Component preserved as a no-op to avoid breaking imports.
export function SocialProofToast() {
  return null;
}
